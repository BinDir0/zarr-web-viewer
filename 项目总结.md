# Zarr 数据集查看器 - 项目总结

## 📦 项目位置

```
/home/guantianrui/zarr-viewer/
```

## 🎯 项目功能

这是一个基于 Flask 的 Web 应用，专门用于查看和标注你的 zarr 格式手部操作数据集。

### 核心功能

1. **多数据集支持** 🗂️
   - 可以同时加载多个 zarr 数据集
   - 按数据集分组显示 episodes
   - 支持启用/禁用数据集

2. **Episode 浏览** 📊
   - 左侧栏显示所有 episodes
   - 按数据集分组，可折叠
   - 显示每个 episode 的帧数

3. **图像显示** 🖼️
   - 显示 episode 的中间帧（默认2帧）
   - 384x384 分辨率
   - 图像自动转换为 base64 传输

4. **手部动作标注** 🤖
   - 手腕位姿（平移 + 6D旋转）
   - 指尖 3D 位置（5个手指，左右手）
   - MANO 参数（45维，左右手）
   - 手的可见性状态（左手/右手/双手）

5. **任务指令** 📝
   - 显示每个 episode 的任务指令
   - 支持多条指令
   - 中英文显示

6. **备注功能** 💬
   - 为每个 episode 添加文字备注
   - 自动保存到 SQLite 数据库
   - 跨数据集独立存储

## 📁 文件结构

```
zarr-viewer/
├── app.py                      # Flask 后端主程序
├── config.yaml                 # 配置文件（需要修改）
├── requirements.txt            # Python 依赖
├── annotations.db             # 标注数据库（自动生成）
│
├── templates/
│   └── index.html             # 主页面 HTML
│
├── static/
│   ├── app.js                 # 前端 JavaScript
│   └── styles.css             # 样式表
│
├── inspect_zarr.py            # 数据集结构检查工具
├── create_demo_dataset.py     # 演示数据集生成工具
├── run.sh                     # 快速启动脚本
│
├── README.md                  # 项目说明
├── QUICK_START.md             # 快速开始指南
├── 使用说明.md                 # 详细使用说明
└── 多数据集配置说明.md         # 多数据集功能说明
```

## 🚀 快速开始

### 1. 修改配置

编辑 `config.yaml`：

```yaml
zarr_datasets:
  - name: "我的数据集1"
    path: /实际/路径/dataset1.zarr
    enabled: true
  - name: "我的数据集2"
    path: /实际/路径/dataset2.zarr
    enabled: true

server_port: 8300
debug_mode: true
```

### 2. 安装依赖

```bash
cd /home/guantianrui/zarr-viewer
pip install -r requirements.txt
```

### 3. 启动服务器

```bash
python app.py
```

或使用：

```bash
./run.sh
```

### 4. 访问网站

浏览器打开：`http://localhost:8300`

## 🔧 工具脚本

### 检查数据集结构

```bash
python inspect_zarr.py /path/to/your/dataset.zarr
```

用途：查看数据集的详细结构，确认路径和数据格式

### 创建演示数据（仅测试用）

```bash
python create_demo_dataset.py ./demo.zarr 10
```

用途：生成一个包含 10 个 episodes 的演示数据集

## 📊 数据集结构

项目适配的 zarr 数据集结构：

```
dataset.zarr/
├── data/
│   ├── action/              # 手部动作
│   │   ├── mano             # (sum_frames, 90) MANO 参数
│   │   ├── wrist            # (sum_frames, 18) 手腕位姿
│   │   ├── fingertips       # (sum_frames, 30) 指尖位置
│   │   └── shape            # (sum_frames, 20) MANO shape
│   ├── state/               # 当前状态（同 action 结构）
│   ├── image                # (sum_frames, 384, 384, 3) RGB 图像
│   ├── depth                # (sum_frames, H, W) 深度图
│   ├── instruction          # (sum_frames, 5) 任务指令
│   ├── instruction_num      # (sum_frames,) 可用指令数
│   ├── presence             # (sum_frames,) 手的可见性
│   ├── intrinsic            # (sum_frames, 4) 相机内参
│   └── extrinsic            # (sum_frames, 16) 相机外参
└── meta/
    ├── episode_name         # (num_episodes,) Episode 名称
    └── episode_ends         # (num_episodes,) Episode 结束索引
```

## 🎨 技术栈

### 后端
- **Flask** 3.1.2 - Web 框架
- **zarr** 3.0.2 - 读取 zarr 数据集
- **numpy** 2.2.6 - 数据处理
- **Pillow** 11.3.0 - 图像处理
- **SQLite** - 标注数据存储

### 前端
- 原生 JavaScript
- CSS3（渐变、动画、响应式）
- Fetch API（异步数据加载）

## 💡 自定义配置

### 修改显示帧数

在 `app.py` 第 241 行左右：

```python
frames = get_middle_frames(episode_data.get("images"), num_frames=2)
```

改为 `num_frames=4` 显示 4 帧

### 修改服务器端口

在 `config.yaml` 中：

```yaml
server_port: 8888  # 改成其他端口
```

### 添加更多数据可视化

1. 在 `app.py` 的 `api_episode()` 函数中添加数据提取
2. 在 `static/app.js` 中添加渲染函数
3. 在 `static/styles.css` 中添加样式

## 📝 数据存储

### 标注数据库结构

```sql
CREATE TABLE annotations (
    id INTEGER PRIMARY KEY,
    episode_id TEXT UNIQUE,        -- 格式: "数据集名::episode索引"
    content TEXT,                  -- 备注内容
    updated_at TEXT                -- 更新时间
);
```

### Episode ID 格式

```
数据集名::episode索引
```

例如：
- `训练数据集::0`
- `验证数据集::5`
- `测试集::10`

这确保了不同数据集的 episodes 不会冲突。

## 🔍 常见问题

### Q: 启动后没有显示 episodes

**A**: 检查：
1. `config.yaml` 中的路径是否正确
2. 数据集是否包含 `meta/episode_name` 和 `meta/episode_ends`
3. 运行 `python inspect_zarr.py <path>` 查看结构

### Q: 某个数据集的 episodes 没有加载

**A**: 
1. 检查该数据集的 `enabled` 是否为 `true`
2. 查看终端的加载日志
3. 确认路径存在且有读取权限

### Q: 图像加载慢

**A**: 
- 图像转换为 base64 需要时间
- 减少显示的帧数（修改 `num_frames`）
- 考虑使用更小的图像分辨率

### Q: 想添加深度图显示

**A**: 
1. 在 `app.py` 中添加深度图数据读取
2. 在前端添加深度图渲染（需要伪彩色映射）
3. 参考图像显示的实现方式

## 🚧 后续扩展方向

- [ ] 视频播放功能（连续播放所有帧）
- [ ] 深度图显示（伪彩色可视化）
- [ ] 3D 手部姿态可视化（Three.js）
- [ ] 数据统计面板
- [ ] 导出标注结果（JSON/CSV）
- [ ] 批量处理和标注
- [ ] 搜索和筛选功能
- [ ] 用户权限管理

## 📚 文档说明

- **README.md** - 项目基本说明
- **QUICK_START.md** - 快速开始指南
- **使用说明.md** - 详细的功能说明和使用教程
- **多数据集配置说明.md** - 多数据集功能的详细文档

## ⚠️ 注意事项

1. **不会修改原始数据**：只读模式打开 zarr 数据集
2. **标注数据独立存储**：在 `annotations.db` 中
3. **多用户注意**：SQLite 不支持高并发，单用户使用最佳
4. **内存占用**：只加载当前查看的 episode，但大图像仍会占用内存

## 🎉 完成功能

✅ 基础框架搭建  
✅ 数据集读取和解析  
✅ Episode 列表显示  
✅ 图像显示  
✅ 手部动作标注显示  
✅ 任务指令显示  
✅ 备注功能  
✅ 多数据集支持  
✅ 数据集分组显示  
✅ 响应式界面设计  

## 🙏 使用提示

1. 首次使用前用 `inspect_zarr.py` 检查数据集结构
2. 在 `config.yaml` 中配置所有需要查看的数据集
3. 使用 `enabled: false` 临时禁用不需要的数据集
4. 定期备份 `annotations.db` 标注数据库
5. 遇到问题查看终端的详细日志

---

**项目创建时间**: 2026-01-30  
**位置**: `/home/guantianrui/zarr-viewer/`  
**状态**: ✅ 可用

祝使用愉快！有任何问题或需要新功能，随时修改代码或询问。

