# Zarr 数据集查看器 - 使用说明

## 项目概述

这是一个专门为你的 zarr 格式手部操作数据集设计的 Web 查看器。可以方便地浏览 episodes、查看图像、检查手部动作标注。

## 快速开始

### 1. 修改配置文件

编辑 `config.yaml`，设置你的数据集路径：

```bash
nano config.yaml
```

修改为：

```yaml
zarr_dataset_path: /path/to/your/zarr/dataset.zarr  # 改成实际路径
server_port: 8300
debug_mode: true
```

### 2. 安装依赖

```bash
cd /home/guantianrui/zarr-viewer
pip install -r requirements.txt
```

### 3. 启动服务器

```bash
python app.py
```

或使用启动脚本：

```bash
./run.sh
```

### 4. 访问网站

在浏览器中打开：
```
http://localhost:8300
```

如果在远程服务器，使用 SSH 端口转发：
```bash
ssh -L 8300:localhost:8300 username@server_address
```

然后在本地浏览器访问 `http://localhost:8300`

## 功能说明

### 界面布局

```
┌──────────────────────────────────────────────────┐
│  Zarr 数据集查看器                                 │
│  数据集路径: /path/to/dataset.zarr                │
└──────────────────────────────────────────────────┘
┌────────────┬─────────────────────────────────────┐
│            │  Episode: episode_0001               │
│ Episodes   │  总帧数: 120  显示帧数: 2            │
│            │  📝 任务指令:                        │
│ episode_0  │  1. 拿起杯子放到桌子上               │
│ episode_1  │  2. Pick up the cup and ...         │
│ episode_2  │  ─────────────────────────────      │
│ ...        │  中间帧图像:                         │
│            │  ┌────────┐ ┌────────┐             │
│            │  │Frame 40│ │Frame 80│             │
│            │  │  图像  │ │  图像  │             │
│            │  └────────┘ └────────┘             │
│            │  ─────────────────────────────      │
│            │  手部动作标注:                       │
│            │  ┌─────────────────────────────┐   │
│            │  │ 帧 #40 的手部动作            │   │
│            │  │ ✓ 左手  ✓ 右手               │   │
│            │  │ 🤚 手腕位姿:                 │   │
│            │  │   左手: 平移(x,y,z) 旋转...  │   │
│            │  │   右手: 平移(x,y,z) 旋转...  │   │
│            │  │ 👆 指尖位置:                 │   │
│            │  │   拇指: (x,y,z) ...          │   │
│            │  └─────────────────────────────┘   │
│            │  ─────────────────────────────      │
│            │  备注:                               │
│            │  ┌──────────────────────────┐       │
│            │  │ 在这里添加备注...         │       │
│            │  └──────────────────────────┘       │
│            │  [保存备注]                          │
└────────────┴─────────────────────────────────────┘
```

### 左侧栏 - Episodes 列表

- 显示所有 episodes（从 `meta/episode_name` 读取）
- 点击选择要查看的 episode
- 选中的 episode 会高亮显示

### 主面板 - Episode 详情

#### 1. **基本信息**
- Episode 名称
- 总帧数
- 当前显示的帧数

#### 2. **任务指令**（如果有）
- 显示该 episode 的所有任务指令
- 支持中英文指令

#### 3. **中间帧图像**
- 默认显示 2 个中间帧（可修改）
- 图像已经调整为 384x384 分辨率
- 显示帧索引号

#### 4. **手部动作标注**

每一帧显示：

**✓ 手的可见性**
- 绿色 ✓: 该手可见
- 红色 ✗: 该手不可见
- 基于 `data/presence` 字段（1=左手, 2=右手, 3=双手）

**🤚 手腕位姿**
- 平移: 3D 坐标 (x, y, z)
- 旋转: 6D 旋转表示
- 分别显示左手和右手

**👆 指尖位置**
- 5 个手指的 3D 坐标
- 拇指、食指、中指、无名指、小指
- 左右手分开显示

**🖐️ MANO 参数**（简略显示）
- 左手 45 维参数
- 右手 45 维参数
- 只显示前几个维度（完整数据太长）

#### 5. **备注区域**
- 可以为每个 episode 添加文字备注
- 点击"保存备注"按钮保存
- 备注自动存储在本地 SQLite 数据库

## 自定义配置

### 修改显示的帧数

在 `app.py` 第 241 行左右找到：

```python
frames = get_middle_frames(episode_data.get("images"), num_frames=2)
```

修改 `num_frames=2` 为你想要的数量，比如：
- `num_frames=4` - 显示 4 帧
- `num_frames=6` - 显示 6 帧

### 修改服务器端口

在 `config.yaml` 中修改：

```yaml
server_port: 8300  # 改成其他端口
```

### 添加更多可视化

如果你想添加更多功能（如深度图显示、3D 可视化等），可以：

1. 在 `app.py` 的 `api_episode()` 函数中添加更多数据
2. 在 `static/app.js` 中添加渲染函数
3. 在 `static/styles.css` 中添加样式
4. 在 `templates/index.html` 中添加 HTML 结构

## 数据存储

### 标注数据库

备注数据存储在 `annotations.db` SQLite 数据库中：

```sql
CREATE TABLE annotations (
    id INTEGER PRIMARY KEY,
    episode_id TEXT UNIQUE,
    content TEXT,
    updated_at TEXT
);
```

可以使用任何 SQLite 工具查看或导出：

```bash
sqlite3 annotations.db "SELECT * FROM annotations;"
```

## 工具脚本

### 1. 检查数据集结构

```bash
python inspect_zarr.py /path/to/your/dataset.zarr
```

用途：查看数据集的详细结构，确认数据路径和形状

### 2. 创建演示数据集（仅用于测试）

```bash
python create_demo_dataset.py ./demo.zarr 10
```

用途：创建一个包含 10 个 episodes 的演示数据集用于测试

## 常见问题

### Q: 启动后显示"未找到任何 episode"

**A**: 检查：
1. `config.yaml` 中的路径是否正确
2. 数据集是否包含 `meta/episode_name` 和 `meta/episode_ends`
3. 运行 `python inspect_zarr.py <path>` 查看结构

### Q: 图像无法显示

**A**: 检查：
1. `data/image` 数组是否存在
2. 图像数据类型是否正确（应该是 uint8）
3. 形状是否为 `(frames, 384, 384, 3)`

### Q: 手部动作数据不显示

**A**: 检查：
1. `data/action/mano`, `data/action/wrist` 等是否存在
2. 数据维度是否正确（mano: 90, wrist: 18, fingertips: 30）

### Q: 加载速度慢

**A**: 
- 图像转换为 base64 需要时间
- 可以减少显示的帧数
- 考虑只加载需要的数据而不是整个 episode

### Q: 想要显示深度图

**A**: 需要修改代码：
1. 在 `app.py` 的 `api_episode()` 中添加深度图处理
2. 在前端添加深度图渲染（可能需要伪彩色映射）

## 下一步扩展建议

- [ ] 添加视频播放功能（连续播放所有帧）
- [ ] 显示深度图（伪彩色）
- [ ] 3D 可视化手部姿态（使用 Three.js）
- [ ] 支持标注筛选和搜索
- [ ] 导出标注结果
- [ ] 批量处理多个 episodes
- [ ] 添加统计信息面板

## 技术支持

如果遇到问题或需要添加新功能，可以：

1. 使用 `inspect_zarr.py` 检查数据集结构
2. 查看浏览器控制台的错误信息
3. 查看终端的 Python 错误输出
4. 修改 `config.yaml` 中的 `debug_mode: true` 查看详细日志

祝使用愉快！🎉

