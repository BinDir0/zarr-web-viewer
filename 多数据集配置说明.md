# 多数据集配置说明

## 功能概述

zarr-viewer 现在支持同时加载和查看多个 zarr 数据集，所有数据集的 episodes 会混合显示在一起，方便对比和查看。

## 配置方式

### 在 config.yaml 中配置多个数据集

```yaml
# Zarr 数据集查看器配置文件

zarr_datasets:
  - name: "训练数据集"
    path: /share_data/zhaoguangyu/dataset_train.zarr
    enabled: true
    
  - name: "验证数据集"
    path: /share_data/zhaoguangyu/dataset_val.zarr
    enabled: true
    
  - name: "测试数据集"
    path: /share_data/zhaoguangyu/dataset_test.zarr
    enabled: true
    
  - name: "旧版数据集"
    path: /share_data/zhaoguangyu/dataset_old.zarr
    enabled: false  # 设置为 false 可以临时禁用某个数据集

server_port: 8300
debug_mode: true
```

### 配置说明

每个数据集配置包含三个字段：

1. **name**: 数据集的显示名称
   - 可以使用中文或英文
   - 用于在界面上区分不同数据集
   - 建议使用有意义的名称

2. **path**: 数据集的完整路径
   - 必须是 .zarr 格式的数据集
   - 使用绝对路径
   - 路径必须存在且可读

3. **enabled**: 是否启用该数据集
   - `true`: 加载该数据集
   - `false`: 跳过该数据集（不加载）
   - 可以用来临时禁用某些数据集而不删除配置

## 界面显示

### 左侧 Episodes 列表

Episodes 会按照数据集分组显示：

```
Episodes
─────────────────────
▼ 训练数据集 (120)
  episode_001   150帧
  episode_002   200帧
  episode_003   180帧
  ...

▼ 验证数据集 (30)
  val_episode_001   160帧
  val_episode_002   190帧
  ...

▼ 测试数据集 (50)
  test_episode_001   170帧
  test_episode_002   185帧
  ...
```

**功能特点：**
- 点击数据集标题可以折叠/展开该数据集
- 每个数据集显示包含的 episode 数量
- Episodes 显示名称和帧数
- 点击 episode 查看详情

### 主面板 Episode 详情

选中 episode 后会显示：
- **数据集来源**: 显示该 episode 来自哪个数据集
- **Episode 名称**: 原始 episode 名称
- **总帧数**: 该 episode 的总帧数
- 其他信息（图像、动作标注等）

## 数据存储

### Episode ID 格式

为了区分来自不同数据集的 episodes，系统使用以下格式的 ID：

```
数据集名称::episode索引
```

例如：
- `训练数据集::0`
- `训练数据集::1`
- `验证数据集::0`
- `测试数据集::0`

这样可以保证即使不同数据集有相同的 episode 索引，也不会冲突。

### 标注数据

每个 episode 的备注会使用上述完整 ID 存储在数据库中，因此：
- 不同数据集的同名 episode 可以有各自的备注
- 标注数据与数据集绑定
- 不会出现标注混淆的问题

## 使用场景

### 场景 1: 对比不同版本的数据集

```yaml
zarr_datasets:
  - name: "v1.0"
    path: /data/dataset_v1.zarr
    enabled: true
  - name: "v2.0"
    path: /data/dataset_v2.zarr
    enabled: true
```

可以方便地在同一界面查看和对比两个版本的数据。

### 场景 2: 分类查看不同任务的数据

```yaml
zarr_datasets:
  - name: "抓取任务"
    path: /data/grasp_dataset.zarr
    enabled: true
  - name: "放置任务"
    path: /data/place_dataset.zarr
    enabled: true
  - name: "倾倒任务"
    path: /data/pour_dataset.zarr
    enabled: true
```

### 场景 3: 临时禁用某些数据集

```yaml
zarr_datasets:
  - name: "当前使用"
    path: /data/current.zarr
    enabled: true
  - name: "旧数据"
    path: /data/old.zarr
    enabled: false  # 临时不看这个
  - name: "测试数据"
    path: /data/test.zarr
    enabled: false  # 临时不看这个
```

## 启动和加载

启动服务器时，系统会：

1. 读取 `config.yaml` 配置
2. 加载所有 `enabled: true` 的数据集
3. 打印加载结果：

```
✓ 成功加载数据集: 训练数据集 (/path/to/dataset1.zarr)
✓ 成功加载数据集: 验证数据集 (/path/to/dataset2.zarr)
⚠️  数据集 '测试数据集' 路径不存在: /path/to/dataset3.zarr
```

## 性能考虑

### 加载时间

- 每个数据集会在启动时打开
- 只读取 meta 信息（episode_name, episode_ends）
- 图像和动作数据按需加载
- 多个小数据集 vs 一个大数据集的性能差异不大

### 内存占用

- 只有当前查看的 episode 数据会加载到内存
- 多数据集不会显著增加内存占用
- 切换 episode 时旧数据会被释放

### 建议

- 如果数据集很多（>10个），考虑禁用暂时不需要的
- 可以根据任务需要随时修改配置并重启服务器
- 配置文件支持注释，方便管理

## 故障排查

### 问题: 某个数据集的 episodes 没有显示

**检查：**
1. 该数据集的 `enabled` 是否为 `true`
2. 路径是否正确
3. 查看终端输出的加载信息
4. 使用 `python inspect_zarr.py <path>` 检查数据集结构

### 问题: Episodes 数量不对

**可能原因：**
1. 某些数据集加载失败但没有注意到
2. 数据集的 `meta/episode_ends` 数据不正确
3. 配置中有数据集被设置为 `enabled: false`

### 问题: 启动时报错

**常见原因：**
1. `config.yaml` 格式错误（缩进、引号等）
2. 路径不存在或没有读取权限
3. zarr 数据集损坏

**解决方法：**
- 检查 YAML 语法
- 确认所有路径都可以访问
- 使用 `inspect_zarr.py` 验证数据集

## 向后兼容

如果你之前使用单数据集配置：

```yaml
zarr_dataset_path: /path/to/dataset.zarr
```

这种方式仍然支持，会被自动转换为：

```yaml
zarr_datasets:
  - name: "默认数据集"
    path: /path/to/dataset.zarr
    enabled: true
```

但建议迁移到新的多数据集格式，功能更强大。

## 示例配置

### 完整示例

```yaml
# Zarr 数据集查看器 - 多数据集配置

zarr_datasets:
  # 主数据集
  - name: "主数据集 v2"
    path: /share_data/zhaoguangyu/main_dataset_v2.zarr
    enabled: true
  
  # 补充数据集
  - name: "补充数据"
    path: /share_data/zhaoguangyu/additional_data.zarr
    enabled: true
  
  # 旧版本（暂时禁用）
  - name: "主数据集 v1"
    path: /share_data/zhaoguangyu/main_dataset_v1.zarr
    enabled: false
  
  # 测试数据
  - name: "测试集"
    path: /share_data/zhaoguangyu/test_dataset.zarr
    enabled: true

# 服务器配置
server_port: 8300
debug_mode: true
```

## 总结

多数据集功能让你可以：

✅ 同时查看多个 zarr 数据集  
✅ 在一个界面对比不同数据集  
✅ 按数据集分组浏览 episodes  
✅ 灵活启用/禁用数据集  
✅ 为每个数据集的 episodes 分别添加标注  

开始使用：
1. 编辑 `config.yaml`，添加你的数据集
2. 运行 `python app.py` 或 `./run.sh`
3. 在浏览器中查看所有数据集的 episodes

